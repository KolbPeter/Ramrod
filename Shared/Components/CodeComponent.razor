@using Ramrod.Data.Models
@if (!string.IsNullOrEmpty(Code.CodeWord))
{
    <div>
        <label>Aktuális kód: </label>
        <table>
            <tr>
                @for (var i = 0; i < 10; i++)
                {
                    <td>@i</td>
                }
            </tr>
            <tr>
                @foreach (var character in Code.CodeWord)
                {
                    <td>@character</td>
                }
            </tr>
        </table>
    </div>
}

@code
{
    [Parameter]
    public RamrodCode Code { get; set; }
}